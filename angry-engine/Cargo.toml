[package]
name = "angry-engine-clean"
version = "0.2.0"
description = "ANGRY Engine Clean - Core + atomic PumpSwap buyback and burn"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "angry_engine_clean"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = "0.29.0"
